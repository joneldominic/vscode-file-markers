# Update README.md to Match v1.5.0 Implementation Plan

## Overview

Update README.md to document all features present in v1.5.0, including the new line highlighting feature (v1.5.0), the enable/disable toggle (v1.2.0), and the updated storage format (V2).

## Current State Analysis

The README documents v1.0.0-era features accurately but is missing:
- Entire Line Highlighting feature (5 commands, 5 highlight types, keybinding, editor context menu, Explorer badge)
- `file-markers.toggleEnabled` command (added v1.2.0)
- `fileMarkers.enabled` setting (added v1.2.0)
- `Ctrl+Shift+H` / `Cmd+Shift+H` keybinding for line highlight toggle
- V2 storage format fields (`lineHighlightTypes`, `lineHighlights`)
- Stats QuickPick action items (Enable/Disable, Open Config)

### Key Discoveries:

- Line highlight types defined at `src/defaults.ts:48-74`
- Line highlight commands at `src/commands.ts:221-458`
- V2 storage format at `src/types.ts:76-82`
- Toggle enabled at `src/extension.ts:49-57`
- Explorer shows `≡` badge for files with line highlights (`src/decorationProvider.ts:117-123`)

## Desired End State

README.md fully documents all features, commands, settings, and keybindings present in v1.5.0. A user reading the README can understand and use every feature the extension provides.

### Verification:
- Every command in `package.json` contributes.commands is documented in README
- Every setting in `package.json` contributes.configuration is documented in README
- Every keybinding in `package.json` contributes.keybindings is documented in README
- Storage format example shows V2 schema
- Line highlighting feature has its own section with usage instructions

## What We're NOT Doing

- Not updating CHANGELOG.md (already auto-generated by release-please)
- Not updating demo GIF/screenshots (would need extension running)
- Not restructuring the overall README layout — keeping existing structure and adding to it
- Not updating CLAUDE.md (separate concern)

## Implementation Approach

Make incremental edits to README.md, section by section, preserving existing content and adding missing documentation.

---

## Phase 1: Update Extension Settings Table

### Overview
Add the missing `fileMarkers.enabled` setting to the Extension Settings table.

### Changes Required:

#### 1. Extension Settings table (README.md ~line 146-149)

**File**: `README.md`
**Changes**: Add `fileMarkers.enabled` row to the settings table

Current:
```markdown
| Setting | Default | Description |
|---------|---------|-------------|
| `fileMarkers.inheritFolderMarkers` | `false` | When enabled, unmarked files inside a marked folder display a dimmed version of the folder's marker |
| `fileMarkers.statusBarAlignment` | `"left"` | Position of marker statistics in the status bar (`"left"` or `"right"`) |
```

New:
```markdown
| Setting | Default | Description |
|---------|---------|-------------|
| `fileMarkers.enabled` | `true` | Enable or disable File Markers. When disabled, markers are hidden but preserved |
| `fileMarkers.inheritFolderMarkers` | `false` | When enabled, unmarked files inside a marked folder display a dimmed version of the folder's marker |
| `fileMarkers.statusBarAlignment` | `"left"` | Position of marker statistics in the status bar (`"left"` or `"right"`) |
```

### Success Criteria:

#### Automated Verification:
- [x] `fileMarkers.enabled` appears in the settings table
- [x] All 3 settings from `package.json` contributes.configuration are present

#### Manual Verification:
- [ ] Table renders correctly in markdown preview

---

## Phase 2: Add Line Highlighting Feature Section

### Overview
Add a new major section documenting the line highlighting feature, inserted after the existing "Keyboard Shortcut" section and before "Status Bar Summary".

### Changes Required:

#### 1. New "Line Highlighting" section (README.md, after line ~55)

**File**: `README.md`
**Changes**: Insert a new feature section

```markdown
### Line Highlighting

Highlight specific lines or ranges in your editor with color-coded backgrounds. Right-click selected text or use the keyboard shortcut to apply highlights.

| Highlight | Color | Use Case |
|-----------|-------|----------|
| Yellow Highlight | Yellow | General emphasis |
| Green Highlight | Green | Approved/good code |
| Blue Highlight | Blue | Reference/informational |
| Red Highlight | Red | Issues/problems |
| Purple Highlight | Purple | Notes/special attention |

- **Set highlight**: Select lines → right-click → **File Markers: Set Line Highlight...**
- **Toggle highlight**: Press `Ctrl+Shift+H` (`Cmd+Shift+H` on macOS) to cycle through highlight colors on the current line
- **Remove highlight**: Select the highlighted lines → right-click → **File Markers: Remove Line Highlight**

Files with line highlights display a `≡` badge in the Explorer panel.
```

### Success Criteria:

#### Automated Verification:
- [x] New section exists between "Keyboard Shortcut" and "Status Bar Summary"
- [x] All 5 highlight types are listed

#### Manual Verification:
- [ ] Section reads clearly and is consistent with existing feature section style

---

## Phase 3: Update Keyboard Shortcut Section

### Overview
Add the line highlight keyboard shortcut alongside the existing marker toggle shortcut.

### Changes Required:

#### 1. Keyboard Shortcut section (README.md ~line 51-55)

**File**: `README.md`
**Changes**: Add line highlight shortcut

Current:
```markdown
### Keyboard Shortcut

Quickly toggle markers on the active file:
- **Windows/Linux**: `Ctrl+Shift+M`
- **macOS**: `Cmd+Shift+M`
```

New:
```markdown
### Keyboard Shortcuts

Quickly toggle markers and highlights:
- **Toggle file marker**: `Ctrl+Shift+M` (`Cmd+Shift+M` on macOS)
- **Toggle line highlight**: `Ctrl+Shift+H` (`Cmd+Shift+H` on macOS)
```

### Success Criteria:

#### Automated Verification:
- [x] Both keybindings from `package.json` are documented

#### Manual Verification:
- [ ] Section reads naturally with both shortcuts

---

## Phase 4: Update Commands Table

### Overview
Add all 6 missing commands to the Commands table.

### Changes Required:

#### 1. Commands table (README.md ~line 160-170)

**File**: `README.md`
**Changes**: Add missing commands, organized into file marker and line highlight groups

New table:
```markdown
| Command | Description |
|---------|-------------|
| `File Markers: Set Marker...` | Add or change a marker on selected files |
| `File Markers: Remove Marker` | Remove marker from selected files |
| `File Markers: Remove Markers in Folder` | Remove all markers from files in a folder |
| `File Markers: Remove All Markers` | Remove all markers in the workspace |
| `File Markers: Toggle Marker` | Cycle through markers on the active file |
| `File Markers: Toggle Enable/Disable` | Enable or disable the extension |
| `File Markers: Set Line Highlight...` | Add a color highlight to selected lines |
| `File Markers: Remove Line Highlight` | Remove highlight from selected lines |
| `File Markers: Remove All Line Highlights in File` | Remove all highlights in the current file |
| `File Markers: Toggle Line Highlight` | Cycle through highlight colors on the current line |
| `File Markers: Remove All Line Highlights` | Remove all line highlights in the workspace |
| `File Markers: Show Marker Statistics` | View marker counts by type |
| `File Markers: Open Configuration` | Open extension settings |
```

### Success Criteria:

#### Automated Verification:
- [x] All 13 commands from `package.json` contributes.commands are present in the table

#### Manual Verification:
- [ ] Commands are logically grouped and easy to scan

---

## Phase 5: Update Storage Format Section

### Overview
Update the Custom Marker Types JSON example to show the V2 format including line highlight fields.

### Changes Required:

#### 1. Custom Marker Types JSON example (README.md ~line 77-98)

**File**: `README.md`
**Changes**: Extend the JSON example to include `lineHighlightTypes` and `lineHighlights`

```json
{
  "markerTypes": [
    {
      "id": "done",
      "badge": "✓",
      "color": "gitDecoration.addedResourceForeground",
      "label": "Done"
    },
    {
      "id": "blocked",
      "badge": "🚫",
      "color": "errorForeground",
      "label": "Blocked"
    }
  ],
  "markers": {
    "src/old-api.ts": "done",
    "src/utils": "blocked"
  },
  "lineHighlightTypes": [
    {
      "id": "highlight-yellow",
      "color": "rgba(255, 235, 59, 0.3)",
      "label": "Yellow Highlight"
    }
  ],
  "lineHighlights": {
    "src/old-api.ts": [
      { "startLine": 10, "endLine": 15, "typeId": "highlight-yellow" }
    ]
  }
}
```

Also add a new properties table for line highlight types:

```markdown
**Line Highlight Type Properties:**

| Property | Required | Description |
|----------|----------|-------------|
| `id` | Yes | Unique identifier used in the `lineHighlights` object |
| `color` | Yes | CSS color value with alpha (e.g., `rgba(255, 235, 59, 0.3)`) |
| `label` | Yes | Display name shown in QuickPick |
```

### Success Criteria:

#### Automated Verification:
- [x] JSON example includes `lineHighlightTypes` and `lineHighlights` fields
- [x] Line highlight type properties table exists

#### Manual Verification:
- [ ] JSON example is valid and matches actual V2 format from `src/types.ts:76-82`

---

## Phase 6: Minor Updates

### Overview
Small touch-ups to other sections for accuracy.

### Changes Required:

#### 1. Status Bar Summary description (README.md ~line 57-59)

**File**: `README.md`
**Changes**: Mention the Enable/Disable and Open Config actions in stats panel

Current:
```markdown
See marker counts at a glance in the status bar. Click to view detailed statistics.
```

New:
```markdown
See marker counts at a glance in the status bar. Click to view detailed statistics, toggle the extension, or open configuration.
```

#### 2. Usage section — add line highlighting usage (README.md ~line 123)

**File**: `README.md`
**Changes**: Add a "Highlighting Lines" subsection after "Keyboard Toggle"

```markdown
### Highlighting Lines

1. Select one or more lines in the editor
2. Right-click → **File Markers: Set Line Highlight...**
3. Choose a highlight color from the QuickPick menu

To remove: select the highlighted lines → right-click → **File Markers: Remove Line Highlight**
```

### Success Criteria:

#### Automated Verification:
- [x] Status bar description mentions toggle and configuration
- [x] Highlighting Lines usage subsection exists

#### Manual Verification:
- [ ] All sections flow naturally together
- [ ] No broken markdown formatting

**Implementation Note**: After completing all phases, do a final pass to verify every command, setting, and keybinding from `package.json` is documented.

---

## Testing Strategy

### Automated:
- Grep README for all 13 command titles from `package.json`
- Grep README for all 3 setting names from `package.json`
- Grep README for both keybinding shortcuts

### Manual Testing Steps:
1. Preview README.md in VSCode markdown preview
2. Verify all tables render correctly
3. Verify all links work
4. Read through as a new user — does the flow make sense?
5. Compare feature list against `package.json` contributes section

## References

- `package.json` — source of truth for commands, settings, keybindings
- `CHANGELOG.md` — feature history
- `src/defaults.ts:48-74` — line highlight type definitions
- `src/types.ts:76-82` — V2 storage format
- `src/commands.ts` — all command implementations
